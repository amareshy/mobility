<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mdoc">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SelectLanguageScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EducationScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MobileOTPVeifyScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DashboardScreen"
            android:label="@string/title_activity_dashboard_screen" />
        <activity
            android:name=".UserRegistrationScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginScreen"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>